name = "redbull-faster-leaderboard"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
# Map UIDs (public, safe to commit)
MAP1_UID = "ne1uhbsWhrUBuveYEx9p08C23Dm"
MAP2_UID = "eLEM0x9TFwpHqnjMPHoesoXTiwk"
MAP3_UID = "SULHiiCvz0g0oIGp1uAyqkLFPoh"
MAP1_NAME = "FASTER OBSTACLE"
MAP2_NAME = "FASTER EVEREST"
MAP3_NAME = "FASTER SKATE"

# Secrets (set via `wrangler secret put`):
# NADEO_LOGIN        — Service account login
# NADEO_PASSWORD     — Service account password
# OAUTH_CLIENT_ID    — OAuth app identifier from api.trackmania.com
# OAUTH_CLIENT_SECRET — OAuth app secret from api.trackmania.com

# KV namespace for caching display names
# Create with: npx wrangler kv namespace create DISPLAY_NAMES
# Then paste the id below
[[kv_namespaces]]
binding = "DISPLAY_NAMES"
id = "16d9719b0cc94de891162f56b92f334a"
